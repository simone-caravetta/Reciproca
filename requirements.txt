selenium>=4.15.0
webdriver-manager>=4.0.0

# For the semantic ranking: scoring how close a candidate's profile reads to the
# niche you described. The app runs without these - the ranking falls back to
# sighting counts alone and says so in the log - so if the install gives trouble on
# your machine, dropping these two is a working setup and not a broken one.
onnxruntime>=1.17.0
tokenizers>=0.15.0
